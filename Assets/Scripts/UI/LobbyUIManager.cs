using UnityEngine;

public class LobbyUIManager : MonoBehaviour
{
    private void Start()
    {
        if (CustomNetworkManager.singleton.relayJoinCode != null)
        {
            
        }
    }
}